# Environment Variables for MemoryPipe

# Path to SQLite database (default: ./memories.db)
MEMORY_PIPE_DB_PATH=./memories.db

# OpenAI API key (optional, for embedding features)
# OPENAI_API_KEY=sk-...

# Maximum number of memories (default: 10000)
# MEMORY_PIPE_MAX_MEMORIES=10000
